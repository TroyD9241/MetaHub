import RequestGrid from "../containers/RequestGrid"

const HomePage = () => {
    return (
        <div class="page">
            <div class="outer-shell">
                HomePAge
                <RequestGrid />
            </div>
        </div>
    )
}

export default HomePage
